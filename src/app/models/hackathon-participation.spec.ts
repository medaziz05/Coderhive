import { HackathonParticipation } from './hackathon-participation';

describe('HackathonParticipation', () => {
  it('should create an instance', () => {
    expect(new HackathonParticipation()).toBeTruthy();
  });
});
